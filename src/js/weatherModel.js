export default class WeatherItemModel {
constructor (cityName, geoPosition, temperature, weatherIcon) {
        this.cityName = cityName;
        this.geoPosition = geoPosition;
        this.temperature = temperature;
        this.weatherIcon = weatherIcon;
    }
}

